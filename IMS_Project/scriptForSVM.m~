trainClass = 'faces_train'
testClass = 'faces_test'

[training, groundTruth] = generateTrainingData(trainClass);
test = loadBins(testClass);

svmopts = ['-c 2 -g 1'];
model = svmtrain(groundTruth, training, 
